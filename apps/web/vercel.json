{
  "framework": "nextjs",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@9.12.3 --activate && pnpm install --filter @prodigal/web... --filter '!harricom-templates' --no-frozen-lockfile",
  "buildCommand": "pnpm build && mkdir -p apps/web/.next && cp -R .next/. apps/web/.next/"
}
