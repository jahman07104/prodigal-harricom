{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @prodigal/web build",
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.3 --activate && pnpm install --filter @prodigal/web... --filter '!harricom-templates' --no-frozen-lockfile",
  "outputDirectory": ".next"
}
